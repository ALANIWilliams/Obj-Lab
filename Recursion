public class Recursion {
 public static void main(String[] args) {
 int result = sum(10);
 System.out.println(result);
 }
 //recursion is calling the same method: sum
 public static int sum(int k) {
 if (k > 0) {
   return k + sum(k - 1);
   } else {
   return 0;
   }
   }
   }
